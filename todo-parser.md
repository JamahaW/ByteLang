# Parser

# TODO: Оптимизация парсера MizLang

## 1. **Базовый класс** ✅

Расширить стрим общими методами для парсинга

~~- [ ] Создать `BaseParser` с общими методами:

- `_skip_newlines()`
- `_consume()`, `_maybe_consume()`, `_consume_set()`
- `_error()`~~

## 2. **Универсальный парсер последовательностей** ✅

- [ ] Реализовать `_parse_sequence()` для замены всех `_parse_comma_separated`
- [ ] Поддержка: пустые списки, trailing commas, пропуск newlines

## 3. **Универсальный парсер блоков** ✅

- [ ] Создать `_parse_block()` для:
    - `{ }` (блоки операторов)
    - `struct { }` (блоки объявлений)
    - С опциональными запятыми

## 5. **Оптимизация диспетчеризации** ✅

- [ ] Убрать ручные проверки `token.type` через if-цепочки

## 6. **Упрощение операторов** ✅

- [ ] Убрать дублирование в `binary_operator()`, `unary_operator()`

## 7. **Унификация функций** ✅

- [ ] Создать `_parse_function_like()` для `fn` и `sig`
- [ ] Получает парсер аргументов

## 8. **Декораторы (опционально)** ⚠️

- [ ] Рассмотреть `@with_backtrack` для сложных конструкций
- [ ] Использовать только если упрощает понимание

## 9. **Структурирование кода** ✅

- [ ] Группировать методы по ответственности:
    - **Expression parsing** (верх)
    - **Statement parsing** (середина)
    - **Helper methods** (низ)

## Цель:

- **Уменьшить на 30-40%** количество строк
- **Сохранить/улучшить** читаемость
- **Упростить** добавление новых конструкций
- **Единообразие** в обработке похожих паттернов

## Принцип:

**DRY (Don't Repeat Yourself)** - если код выглядит похоже, нужно объединить.
**KISS (Keep It Simple, Stupid)** - не усложнять без необходимости.